/*package com.book;

public class BookData {
    public class Solution {
    public static void main(String[] args) throws IOException {

        List<Integer> bookData = new ArrayList<>();
        for (String line : Files.readAllLines(Paths.get("src/main/resources/hospitalSketches.txt"))){
           for (String part : line.split("\\s+")){
               Integer i = Integer.valueOf(part);
               bookData.add(i);
           }
        }

        System.out.println(bookData);

    }
}

List<String> bookData = Files.readAllLines(Paths.get("src/main/resources/hospitalSketches.txt"));
}*/

        /*List<String> duplicates = new ArrayList<>();
        for (int i = 0; i < bookDataSpliter.length; i++){
            for (int j = 0; j < bookDataSpliter.length; j++){
                if (i == j) continue;
                if (bookDataSpliter[i].equals(bookDataSpliter[j])){
                    duplicates.add(bookDataSpliter[i]);
                }
            }
        }
        System.out.println(duplicates);*/
